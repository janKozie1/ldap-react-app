const DEF_URL = '/data/data'
const BASE_URL = ''
const DEF_PARAMS = {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    }
}
const DEF_QUERY = null

export const fetchDefConfig = {
    DEF_URL,
    DEF_PARAMS,
    DEF_QUERY,
    BASE_URL
}
//http://127.0.0.1:8080
